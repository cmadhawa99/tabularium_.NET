<UserControl x:Class="ArchivumWpf.Views.DocumentManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AllowDrop="True" Drop="UserControl_Drop">
    
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header: RR Number and Name -->
        <TextBlock Grid.Row="0" Text="{Binding RecordTitle}" 
                   FontSize="18" FontWeight="Bold" Margin="10"/>

        <!-- Document Icons Area with ScrollBars -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Files}" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.ViewDocumentCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                CommandParameter="{Binding}" 
                                Margin="10" Width="80" Height="100">
                            <StackPanel>
                                <!-- Placeholder for Document Icon -->
                                <Rectangle Width="40" Height="50" Fill="LightGray" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding OriginalFileName}" TextTrimming="CharacterEllipsis" Width="70" TextAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>